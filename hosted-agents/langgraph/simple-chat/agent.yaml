name: simple-chat
description: The simplest LangGraph agent - just calls an LLM and returns the response.
metadata:
  example:
    - role: user
      content: "Hello, what can you help me with?"
  tags:
    - example
    - learning
template:
  name: SimpleChatAgent
  kind: hosted
  protocols:
    - protocol: responses
      version: v1
  environment_variables:
    - name: AZURE_OPENAI_ENDPOINT
      value: ${AZURE_OPENAI_ENDPOINT}
    - name: AZURE_AI_MODEL_DEPLOYMENT_NAME
      value: "{{chat}}"
resources:
  - kind: model
    id: gpt-4.1
    name: chat
